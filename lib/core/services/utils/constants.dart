class DemoConstants {
  static const smallDuration = const Duration(milliseconds: 300);
  static const mediumDuration = const Duration(milliseconds: 800);
  static const largeDuration = const Duration(milliseconds: 1500);
}

class URLs {
  static const rules = 'https://google.com';
}
