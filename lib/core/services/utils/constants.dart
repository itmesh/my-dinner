class DemoConstants {
  static const Duration smallDuration = const Duration(milliseconds: 300);
  static const Duration mediumDuration = const Duration(milliseconds: 800);
  static const Duration largeDuration = const Duration(milliseconds: 1500);
}
